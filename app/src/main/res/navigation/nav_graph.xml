<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentoAgregar">

    <fragment
        android:id="@+id/fragmentoAgregar"
        android:name="com.example.mypractica.FragmentoAgregar"
        android:label="fragment_fragmento_agregar"
        tools:layout="@layout/fragment_fragmento_agregar" >
        <action
            android:id="@+id/action_fragmentoAgregar_to_fragmentListado"
            app:destination="@id/fragmentListado" />
    </fragment>
    <fragment
        android:id="@+id/fragmentListado"
        android:name="com.example.mypractica.FragmentListado"
        android:label="fragment_listado"
        tools:layout="@layout/fragment_listado" />
</navigation>